@model VillaOy.Models.Postitoimipaikat

@{
    ViewBag.Title = "Delete";
}

<h2>Poista postitoimipaikka</h2>

<div>
    <hr />
    <dl class="dl-horizontal">
        <dt>
            @Html.DisplayNameFor(model => model.Postitoimipaikka)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Postitoimipaikka)
        </dd>

    </dl>

    @using (Html.BeginForm())
    {
        @Html.AntiForgeryToken()

        /*<div class="form-actions no-color">
            <input type="submit" value="Poista" class="btn btn-danger" /> |
            @Html.ActionLink("Takaisin listaan", "Index")
        </div>*/
        <div class="form-actions no-color">
            <button type="submit" value="Poista" class="btn btn-danger">
                <i class="fa fa-exclamation-triangle"></i> Poista
            </button> |
            @Html.ActionLink("Takaisin listaan", "Index")
        </div>
    }
</div>
